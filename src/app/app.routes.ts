import { provideRouter, Routes } from '@angular/router';
import { CheckboxComponent } from './checkbox/checkbox.component';

export const routes: Routes = [
  { title: 'Angular/Checkbox', component: CheckboxComponent, path: 'checkbox' },
];
